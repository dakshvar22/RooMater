angular
  .module( 'myModule', [ 'tc.chartjs' ])
  .controller( 'myController', function( $scope ) {

    // Chart.js Data
    $scope.data = { /* options */ }

    // Chart.js Options
    $scope.options = { /* data */ };

  });
